import React from 'react';

const TodoList = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        width: '100%',
      }}
    >
      <div style={{ color: '#fff', fontSize: '2rem' }}>TodoList</div>
    </div>
  );
};

export default TodoList;

